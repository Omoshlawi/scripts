# name.py

<!-- One or two sentences: what it does, and where the output goes. -->

```sh
./name.py input                      # the common case
./name.py --flag value input         # a fuller example
```

<!-- Delete any section below that doesn't apply to this script. -->

## First run

<!-- Only if the script bootstraps a .venv. Show the message the user will see. -->

## Options

| Flag | Meaning |
| --- | --- |
| `input` | <!-- positional --> |
| `-x`, `--example VALUE` | <!-- what it does, and what happens when omitted --> |
| `-h`, `--help` | Usage. |

## Interactive mode

<!-- If it prompts: paste a real transcript, not an invented one. -->

```
$ ./name.py input
```

## Output

<!-- Where files land, default names, overwrite behaviour. -->

## Notes and caveats

<!-- Anything surprising: advisory guarantees, platform limits, destructive edges. -->

## Exit codes

| Code | Meaning |
| --- | --- |
| 0 | Success. |
| 1 | <!-- expected failures --> |
| 2 | Bad usage (argparse). |

## Troubleshooting

**`error message here`** — <!-- what it means and what to do about it -->
